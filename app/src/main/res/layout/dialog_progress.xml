<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:theme="@style/CustomAlertDialogStyle">

    <ProgressBar
        android:id="@+id/progress_circular_dialog"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_marginBottom="16dp"
        android:indeterminateTint="@color/kPrimaryColor"
        android:layout_centerInParent="true"
        android:indeterminateTintMode="src_in"/>

    <TextView
        android:layout_below="@+id/progress_circular_dialog"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="@string/authntificate"
        android:layout_centerHorizontal="true"
        style="@style/TextAppearance.AppCompat.Medium"
        android:paddingTop="8dp" />

</RelativeLayout>
